import ReactModal from 'react-modal'

export function SignUpModal() {
  return (
    <ReactModal
      isOpen={true}
      // overlayClassName='react-modal-overlay'
    >
        React Modal

    </ReactModal>
  )
}